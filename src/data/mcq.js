const DSA_REF = {
  "Big-O": {
    title: "Big-O & Time Complexity",
    points: [
      "O(1) — constant: array index access, hash lookup",
      "O(log n) — input halves each step: binary search, BST lookup",
      "O(n) — linear scan: simple loop through array",
      "O(n log n) — divide & conquer: merge sort, quick sort (avg)",
      "O(n²) — nested loops: bubble sort, selection sort",
      "Drop constants: O(2n) → O(n). Dominant term wins: O(n² + n) → O(n²)",
    ],
    formula: null,
  },
  Recursion: {
    title: "Recursion & Call Stack",
    points: [
      "Every recursive function needs a BASE CASE to stop",
      "Without base case → infinite recursion → Stack Overflow",
      "Each call adds a new stack frame to the call stack",
      "Stack unwinds (returns) once base case is reached",
      "Fibonacci (naïve): O(2ⁿ) — exponential due to repeated subproblems",
      "Memoization turns O(2ⁿ) → O(n) by caching results",
    ],
    formula: "fib(n) = fib(n-1) + fib(n-2),  base: fib(0)=0, fib(1)=1",
  },
  Hanoi: {
    title: "Tower of Hanoi",
    points: [
      "Rules: move n disks from source to destination using auxiliary peg",
      "Only one disk moved at a time; larger disk cannot sit on smaller",
      "Minimum moves for n disks = 2ⁿ − 1",
      "Time complexity: O(2ⁿ) — exponential",
      "Classic example of exponential recursion",
    ],
    formula: "T(n) = 2T(n-1) + 1  →  T(n) = 2ⁿ − 1",
  },
  Sorting: {
    title: "Merge Sort & Quick Sort",
    points: [
      "Merge Sort: divide in half recursively, then merge → O(n log n) always",
      "Quick Sort: pick pivot, partition array → O(n log n) avg, O(n²) worst",
      "Quick Sort worst case: sorted/reverse-sorted array with bad pivot",
      "Merge Sort is stable; Quick Sort is not (in-place)",
      "Both use divide-and-conquer strategy",
    ],
    formula: null,
  },
  Queue: {
    title: "Queue — FIFO",
    points: [
      "FIFO: First In, First Out — like a queue at a ticket counter",
      "Enqueue (add to rear) / Dequeue (remove from front) — both O(1)",
      "Used in: BFS traversal, printer queues, task scheduling",
      "Deque: double-ended queue — insert/remove from both ends",
      "Circular queue avoids wasted space in array implementation",
    ],
    formula: null,
  },
  Stack: {
    title: "Stack — LIFO",
    points: [
      "LIFO: Last In, First Out — like a stack of plates",
      "Push / Pop / Peek — all O(1)",
      "Valid Parentheses: push open bracket, pop and check on close bracket",
      "Call stack uses LIFO — most recent function call is on top",
      "Used in: undo operations, expression evaluation, DFS traversal",
    ],
    formula: null,
  },
  Trees: {
    title: "Binary Trees & BST",
    points: [
      "BST property: left subtree < root < right subtree (all nodes)",
      "Height = number of EDGES in longest path from root to leaf",
      "Balanced tree: left and right subtree heights differ by ≤ 1",
      "Leaf node: no children (both left and right are NULL)",
      "BST search/insert avg: O(log n); worst (skewed): O(n)",
    ],
    formula: null,
  },
  Traversals: {
    title: "Tree Traversals",
    points: [
      "Inorder (L→Root→R): gives nodes in ascending sorted order for BST",
      "Preorder (Root→L→R): used to create a copy of the tree",
      "Postorder (L→R→Root): used to delete/free a tree",
      "Level-order (BFS): uses a Queue, visits level by level",
      "DFS traversals (in/pre/post) use Stack (or recursion)",
    ],
    formula: null,
  },
  "Two-Pointer": {
    title: "Two-Pointer & Sliding Window",
    points: [
      "Two-pointer: use two indices moving toward each other or in same direction",
      "Sliding window: maintain a window of size k, slide one step at a time",
      "Sliding window reduces O(n·k) brute force → O(n) linear",
      "Floyd's cycle detection: slow moves 1 step, fast moves 2 steps",
      "If slow and fast pointers meet → cycle exists in linked list",
    ],
    formula: null,
  },
};

const AFD_REF = {
  useEffect: {
    title: "useEffect & Lifecycle",
    points: [
      "Replaces componentDidMount, componentDidUpdate, componentWillUnmount",
      "No deps array → runs after EVERY render",
      "Empty deps [] → runs ONCE on mount only",
      "Deps [val] → runs when val changes",
      "Return a cleanup function to clear timers/subscriptions",
    ],
    formula: "useEffect(() => { /* effect */ return () => { /* cleanup */ }; }, [deps]);",
  },
  useState: {
    title: "useState Hook",
    points: [
      "Returns an array: [currentState, setterFunction]",
      "State updates are asynchronous — never mutate state directly",
      "Safe update with previous value: setCount(prev => prev + 1)",
      "Each useState call is independent; order must stay consistent",
      "Initial value only used on first render",
    ],
    formula: "const [state, setState] = useState(initialValue);",
  },
  Forms: {
    title: "Controlled Components & Forms",
    points: [
      "Controlled: form element value driven by React state via value={state}",
      "Uncontrolled: form uses its own internal DOM state (ref-based)",
      "Always use onChange={handler} to update state on input change",
      "e.preventDefault() stops default form submission/page reload",
      "Validation: check state before submit or use libraries like Formik",
    ],
    formula: "<input value={state} onChange={e => setState(e.target.value)} />",
  },
  "React Core": {
    title: "React Core Concepts",
    points: [
      "Virtual DOM: lightweight copy of real DOM; React diffs and patches",
      "JSX compiles to React.createElement(type, props, ...children)",
      "children prop: elements passed between component opening/closing tags",
      "Synthetic Event: cross-browser wrapper — same API for all browsers",
      ".map() is the standard way to render lists; always add key prop",
    ],
    formula: null,
  },
  Router: {
    title: "React Router v6",
    points: [
      "<Link to='/path'> navigates without full page reload (no <a href>)",
      "<Route path='/x' element={<Comp />} /> renders on matching path",
      "useParams() → extract :id from URL like /users/:id",
      "<Outlet /> renders nested child routes in layout components",
      "useNavigate() for programmatic navigation",
    ],
    formula: null,
  },
  "Node/Express": {
    title: "Node.js & Express",
    points: [
      "req.params.id → from URL pattern /users/:id (dynamic segment)",
      "req.query.page → from query string /users?page=2",
      "req.body → from POST/PUT request body (needs express.json() middleware)",
      "express.json() parses incoming JSON payload into req.body",
      "Middleware chain: req → middleware1 → middleware2 → route handler",
    ],
    formula: "app.use(express.json());  // must be before routes",
  },
  "Event Loop": {
    title: "JS Event Loop & Web APIs",
    points: [
      "Call Stack: executes synchronous code, one frame at a time",
      "Web APIs (browser): handle setTimeout, fetch, DOM events in background",
      "Microtask queue (Promises): processed before macrotask queue",
      "Macrotask queue: setTimeout, setInterval callbacks",
      "Node.js: single-threaded but non-blocking via event-driven I/O model",
    ],
    formula: null,
  },
  Auth: {
    title: "Authentication & JWT",
    points: [
      "JWT = header.payload.signature — base64 encoded, dot-separated",
      "Stateless: server doesn't store session; token carries all info",
      "bcrypt hashes passwords with salt rounds (cost factor)",
      "Never store plain passwords; compare with bcrypt.compare()",
      "JWT verified with secret key — tampered tokens fail verification",
    ],
    formula: "Authorization: Bearer <token>",
  },
};

const FOML_REF = {
  Regression: {
    title: "Linear Regression",
    points: [
      "y = β₀ + β₁x  →  β₀ = intercept, β₁ = slope",
      "MSE = (1/n)Σ(y − ŷ)² — squares errors, penalizes large errors more",
      "MAE = (1/n)Σ|y − ŷ| — absolute errors, more robust to outliers",
      "RMSE = √MSE — same units as target variable",
      "R² = 1 means perfect fit; R² = 0 means model explains nothing",
    ],
    formula: "β₁ = [nΣxy − ΣxΣy] / [nΣx² − (Σx)²]   β₀ = ȳ − β₁x̄",
  },
  "Gradient Descent": {
    title: "Gradient Descent",
    points: [
      "Iteratively adjusts parameters to minimize the loss function",
      "Learning rate α: too small → slow convergence; too large → overshoots",
      "Update rule: θ_new = θ − α × ∂Loss/∂θ",
      "Batch GD: uses all data per step (stable, slow on large data)",
      "Stochastic GD (SGD): one sample per step (noisy, faster)",
      "Mini-batch GD: small batches — balance of speed and stability",
    ],
    formula: "θ = θ − α · ∇J(θ)",
  },
  "Bias-Variance": {
    title: "Bias-Variance Tradeoff",
    points: [
      "High Bias → underfitting: model too simple, misses patterns",
      "High Variance → overfitting: model learns noise, fails on new data",
      "Goal: find sweet spot where both bias and variance are low",
      "Regularization (L1/L2) adds penalty to reduce variance (overfitting)",
      "More training data reduces variance but doesn't fix high bias",
    ],
    formula: "Total Error = Bias² + Variance + Irreducible Noise",
  },
  Probability: {
    title: "Probability & Distributions",
    points: [
      "Discrete RV: countable outcomes (coin flips, dice) — use PMF",
      "Continuous RV: infinite outcomes (height, temperature) — use PDF",
      "PMF: P(X=x) for each outcome; all probabilities must sum to 1",
      "PDF: probability = area under curve; total area = 1",
      "Normal distribution: bell curve, symmetric, mean = median = mode",
    ],
    formula: "P(a ≤ X ≤ b) = ∫ₐᵇ f(x)dx  (for continuous RV)",
  },
  NumPy: {
    title: "NumPy Arrays",
    points: [
      "ndarray: N-dimensional array, all elements same type",
      "ndim: number of dimensions (1=vector, 2=matrix, 3=tensor)",
      "shape: tuple of size per dimension e.g. (3, 4) = 3 rows, 4 cols",
      "Vectorized operations: no Python loops, runs in compiled C — fast",
      "Broadcasting: operations on arrays of different shapes (auto-expands)",
    ],
    formula: "import numpy as np\narr = np.array([1, 2, 3])\narr * 2  # → [2, 4, 6]",
  },
  Pandas: {
    title: "Pandas DataFrames",
    points: [
      "DataFrame: 2D labeled table — rows and columns with index",
      "pd.read_csv() / pd.read_excel() / pd.read_json() for file input",
      "Pandas cannot natively read binary image files (.jpg, .png)",
      "groupby(): split-apply-combine for aggregation",
      "df.merge() / pd.concat() for joining DataFrames",
    ],
    formula: "df = pd.read_csv('file.csv')\ndf.groupby('col').mean()",
  },
};

const DBMS_REF = {
  "MongoDB Foundations": {
    title: "MongoDB Foundations",
    points: [
      "NoSQL, document-oriented database — stores data as BSON documents",
      "BSON = Binary JSON — supports more data types than JSON",
      "Collection = group of documents (like a SQL table)",
      "_id field auto-created as unique ObjectId for every document",
      "Flexible schema — documents in same collection can differ in structure",
    ],
    formula: null,
  },
  "MongoDB CRUD": {
    title: "MongoDB CRUD Operations",
    points: [
      "insertOne({}) / insertMany([{},{}]) — create documents",
      "find({query}) / findOne({query}) — read with query operators",
      "updateOne({filter}, {$set:{...}}) / updateMany() — update",
      "deleteOne({filter}) / deleteMany({filter}) — delete",
      "$lt (less than), $gt (greater than), $eq, $ne, $in operators",
    ],
    formula: "db.users.find({ age: { $gt: 18 } })",
  },
  "MongoDB Performance": {
    title: "MongoDB Aggregation & Indexes",
    points: [
      "_id index auto-created on every collection (unique by default)",
      "Compound index: multiple fields — improves complex query performance",
      "explain() — shows query execution plan and index usage",
      "Aggregation pipeline: $match → $group → $project → $sort",
      "$match first in pipeline = filter early = better performance",
      "$unwind: deconstructs array field → one document per array element",
      "$lookup: left outer join between two collections",
    ],
    formula: "db.orders.aggregate([{$match:{status:'A'}}, {$group:{_id:'$cust_id'}}])",
  },
  Transactions: {
    title: "Transactions & ACID",
    points: [
      "ACID: Atomicity, Consistency, Isolation, Durability",
      "Atomicity: all operations succeed or ALL are rolled back (all-or-nothing)",
      "COMMIT: permanently saves all changes in the transaction",
      "ROLLBACK: undoes all changes back to the state before transaction",
      "Isolation: concurrent transactions don't interfere with each other",
      "Durability: committed data persists even after system crash",
    ],
    formula: null,
  },
  "SQL Basics": {
    title: "SQL Basics",
    points: [
      "SELECT col FROM table WHERE condition ORDER BY col DESC LIMIT n",
      "DISTINCT: removes duplicate rows from result set",
      "LIMIT n: returns only first n rows",
      "ORDER BY col ASC/DESC: sorts results ascending or descending",
      "DDL: CREATE/ALTER/DROP | DML: SELECT/INSERT/UPDATE/DELETE",
    ],
    formula: "SELECT DISTINCT name FROM students ORDER BY age DESC LIMIT 10;",
  },
  "SQL Advanced": {
    title: "SQL Joins & Aggregation",
    points: [
      "INNER JOIN: returns only rows matching in BOTH tables",
      "LEFT JOIN: all rows from left table + matching from right (NULLs if no match)",
      "GROUP BY: groups rows with same values for aggregate functions",
      "HAVING: filters groups AFTER GROUP BY (WHERE filters rows before)",
      "Aggregate functions: COUNT(), SUM(), AVG(), MAX(), MIN()",
    ],
    formula: "SELECT dept, COUNT(*) FROM emp GROUP BY dept HAVING COUNT(*) > 5;",
  },
};

export const MCQ_DATA = {
  DSA: [
    {
      id: "dsa-1", subject: "DSA", topic: "Big-O",
      question: "What is the time complexity of accessing an element at a specific index in an array?",
      options: ["O(n)", "O(1)", "O(log n)", "O(n²)"],
      correct: 1,
      explanation: "Array index access is O(1) — the memory address is computed directly using base address + index × element size, regardless of array size.",
      reference: DSA_REF["Big-O"],
    },
    {
      id: "dsa-2", subject: "DSA", topic: "Big-O",
      question: "Binary Search can be applied to which type of input?",
      options: ["Any unsorted array", "Only sorted arrays with search space reduction", "Arrays with duplicate elements only", "Linked lists without preprocessing"],
      correct: 1,
      explanation: "Binary search requires a sorted array. It works by comparing the target with the middle element and halving the search space each step — this only works when the array is sorted.",
      reference: DSA_REF["Big-O"],
    },
    {
      id: "dsa-3", subject: "DSA", topic: "Recursion",
      question: "What happens if a recursive function does NOT have a base case?",
      options: ["The program runs faster", "Stack Overflow Error — infinite recursion", "The program auto-terminates after n iterations", "The program runs but produces incorrect results"],
      correct: 1,
      explanation: "Without a base case, the function calls itself indefinitely. Each call adds a new frame to the call stack. When the stack runs out of memory, a Stack Overflow Error occurs.",
      reference: DSA_REF["Recursion"],
    },
    {
      id: "dsa-4", subject: "DSA", topic: "Recursion",
      question: "What is the time complexity of the nth Fibonacci number using naive recursion (no memoization)?",
      options: ["O(n)", "O(log n)", "O(2ⁿ)", "O(n²)"],
      correct: 2,
      explanation: "Naive Fibonacci creates two recursive calls per invocation, forming a binary tree of calls. The tree has ~2ⁿ nodes, giving O(2ⁿ) time complexity.",
      reference: DSA_REF["Recursion"],
    },
    {
      id: "dsa-5", subject: "DSA", topic: "Hanoi",
      question: "Tower of Hanoi with n disks — what is the minimum number of moves required?",
      options: ["n", "n²", "2ⁿ − 1", "2ⁿ"],
      correct: 2,
      explanation: "The recurrence T(n) = 2T(n-1) + 1 solves to T(n) = 2ⁿ − 1. For example, 3 disks need 7 moves, 4 disks need 15 moves.",
      reference: DSA_REF["Hanoi"],
    },
    {
      id: "dsa-6", subject: "DSA", topic: "Sorting",
      question: "What is the worst-case time complexity of Merge Sort?",
      options: ["O(n)", "O(n log n)", "O(n²)", "O(2ⁿ)"],
      correct: 1,
      explanation: "Merge Sort divides the array in half (log n levels) and merges each level in O(n) time. This gives O(n log n) in all cases — best, average, and worst.",
      reference: DSA_REF["Sorting"],
    },
    {
      id: "dsa-7", subject: "DSA", topic: "Sorting",
      question: "What is the worst-case time complexity of Quick Sort?",
      options: ["O(n)", "O(n log n)", "O(n²)", "O(log n)"],
      correct: 2,
      explanation: "Quick Sort's worst case occurs when the pivot is always the smallest or largest element (e.g., on a sorted array with first-element pivot), creating O(n) partitions of O(n) each → O(n²).",
      reference: DSA_REF["Sorting"],
    },
    {
      id: "dsa-8", subject: "DSA", topic: "Queue",
      question: "Which data structure follows the FIFO (First In, First Out) principle?",
      options: ["Stack", "Queue", "Array", "Linked List"],
      correct: 1,
      explanation: "A Queue follows FIFO — the first element added is the first to be removed, like people waiting in a line. Enqueue adds to the rear, dequeue removes from the front.",
      reference: DSA_REF["Queue"],
    },
    {
      id: "dsa-9", subject: "DSA", topic: "Stack",
      question: "To validate whether brackets like '(){}[]' are properly matched, which data structure is most efficient?",
      options: ["Array", "Linked List", "Stack", "Queue"],
      correct: 2,
      explanation: "A Stack (LIFO) is ideal: push every opening bracket, and when a closing bracket is encountered, pop and check if it matches. If the stack is empty at the end, brackets are valid.",
      reference: DSA_REF["Stack"],
    },
    {
      id: "dsa-10", subject: "DSA", topic: "Trees",
      question: "In a Binary Search Tree (BST), which statement is ALWAYS true?",
      options: ["Left subtree values > root", "Right subtree values < root", "Left subtree < root AND right subtree > root", "All nodes have exactly two children"],
      correct: 2,
      explanation: "The BST property: every node in the left subtree has a value less than the root, and every node in the right subtree has a value greater than the root. This applies recursively to all subtrees.",
      reference: DSA_REF["Trees"],
    },
    {
      id: "dsa-11", subject: "DSA", topic: "Traversals",
      question: "Inorder traversal (Left → Root → Right) of a Binary Search Tree produces?",
      options: ["Values in descending order", "Values in ascending sorted order", "Values in insertion order", "Random order"],
      correct: 1,
      explanation: "Inorder traversal visits left subtree first (all smaller values), then root, then right subtree (all larger values). For a BST, this always yields values in ascending sorted order.",
      reference: DSA_REF["Traversals"],
    },
    {
      id: "dsa-12", subject: "DSA", topic: "Trees",
      question: "The height of a binary tree is defined as?",
      options: ["Number of nodes in the longest path from root to leaf", "Number of EDGES in the longest path from root to leaf", "Total number of nodes in the tree", "Depth of the root node"],
      correct: 1,
      explanation: "Height = number of edges (not nodes) in the longest path from root to a leaf node. A tree with a single node has height 0. A null tree has height -1.",
      reference: DSA_REF["Trees"],
    },
    {
      id: "dsa-13", subject: "DSA", topic: "Trees",
      question: "A Height-Balanced Binary Tree requires the left and right subtree heights to differ by at most?",
      options: ["0", "1", "2", "n/2"],
      correct: 1,
      explanation: "A height-balanced (AVL) tree maintains |height(left) - height(right)| ≤ 1 for every node. This keeps search, insert, delete at O(log n) guaranteed.",
      reference: DSA_REF["Trees"],
    },
    {
      id: "dsa-14", subject: "DSA", topic: "Two-Pointer",
      question: "The Sliding Window technique reduces finding max-sum subarray of size k from O(n·k) to?",
      options: ["O(k²)", "O(n)", "O(n log n)", "O(2ⁿ)"],
      correct: 1,
      explanation: "Instead of recomputing the sum for each window (O(n·k)), the sliding window adds the new element and removes the old element from the running sum — just two operations per step, giving O(n).",
      reference: DSA_REF["Two-Pointer"],
    },
    {
      id: "dsa-15", subject: "DSA", topic: "Trees",
      question: "In a binary tree, a leaf node is defined as?",
      options: ["A node with only a left child", "A node with only a right child", "A node with both children", "A node with NO children (both left and right are NULL)"],
      correct: 3,
      explanation: "A leaf node has no children — both its left and right pointers are NULL. It represents the end of a path from the root.",
      reference: DSA_REF["Trees"],
    },
  ],

  AFD: [
    {
      id: "afd-1", subject: "AFD", topic: "useEffect",
      question: "Which React hook replaces componentDidMount and componentDidUpdate?",
      options: ["useMemo", "useEffect", "useContext", "useState"],
      correct: 1,
      explanation: "useEffect runs after every render by default (like componentDidUpdate) and on mount (like componentDidMount). Pass [] as deps to run only on mount, or return a function to replicate componentWillUnmount.",
      reference: AFD_REF["useEffect"],
    },
    {
      id: "afd-2", subject: "AFD", topic: "useState",
      question: "What does useState return?",
      options: ["An object with state and a function", "A boolean", "An array: [currentState, updateFunction]", "A string"],
      correct: 2,
      explanation: "useState returns a 2-element array. By convention we destructure it: const [count, setCount] = useState(0). The first element is the current state value, the second is the setter function.",
      reference: AFD_REF["useState"],
    },
    {
      id: "afd-3", subject: "AFD", topic: "Forms",
      question: "A controlled component in React is?",
      options: ["A component controlled by Redux", "A form element whose value is controlled by React state", "A component that has no state", "A strict mode component"],
      correct: 1,
      explanation: "Controlled components keep form data in React state. The input value attribute is bound to state (value={state}), and onChange updates that state. React is the 'single source of truth' for the form data.",
      reference: AFD_REF["Forms"],
    },
    {
      id: "afd-4", subject: "AFD", topic: "React Core",
      question: "What is the purpose of the children prop in React?",
      options: ["Pass functions to child elements", "Pass elements directly inside a component's opening and closing tags", "Create arrays", "Map through lists"],
      correct: 1,
      explanation: "The children prop contains whatever JSX you place between the component's opening and closing tags: <Card><p>content</p></Card>. Inside Card, props.children renders that <p> element.",
      reference: AFD_REF["React Core"],
    },
    {
      id: "afd-5", subject: "AFD", topic: "React Core",
      question: "What is a 'Synthetic Event' in React?",
      options: ["An event triggered by automated testing", "A cross-browser wrapper around the browser's native event with consistent properties", "A fake event to bypass the Virtual DOM", "An event that only triggers in strict mode"],
      correct: 1,
      explanation: "React wraps native browser events in a SyntheticEvent object that normalises differences between browsers. It implements the same interface as native events (stopPropagation, preventDefault, etc.) but works consistently everywhere.",
      reference: AFD_REF["React Core"],
    },
    {
      id: "afd-6", subject: "AFD", topic: "useEffect",
      question: "What is the purpose of the dependency array in useEffect?",
      options: ["To import modules", "To control when the effect re-runs", "To list external APIs", "To define state variables"],
      correct: 1,
      explanation: "The dependency array tells React when to re-run the effect. [] = run once on mount. [val] = run when val changes. Omit array = run after every render. This prevents infinite loops and unnecessary work.",
      reference: AFD_REF["useEffect"],
    },
    {
      id: "afd-7", subject: "AFD", topic: "Router",
      question: "Which React Router DOM component prevents full page reloads when navigating?",
      options: ["<Anchor>", "<a>", "<Link>", "<Navigate>"],
      correct: 2,
      explanation: "<Link to='/path'> uses the History API to update the URL without reloading the page, enabling Single Page Application navigation. Regular <a href> causes a full page reload.",
      reference: AFD_REF["Router"],
    },
    {
      id: "afd-8", subject: "AFD", topic: "Node/Express",
      question: "How do you extract the dynamic ID from the URL /users/:id in Express?",
      options: ["req.body.id", "req.query.id", "req.params.id", "req.header.id"],
      correct: 2,
      explanation: "Named route parameters like :id are accessible via req.params.id. req.query is for query strings (?id=1), req.body is for POST body data, req.headers is for HTTP headers.",
      reference: AFD_REF["Node/Express"],
    },
    {
      id: "afd-9", subject: "AFD", topic: "Node/Express",
      question: "What is the purpose of express.json() middleware?",
      options: ["Convert database objects to JSON", "Parse incoming requests with JSON payloads and populate req.body", "Send JSON responses back to the client", "Format server logs"],
      correct: 1,
      explanation: "express.json() is built-in Express middleware that reads the request body, parses the JSON string, and attaches the result to req.body. Without it, req.body is undefined for JSON POST requests.",
      reference: AFD_REF["Node/Express"],
    },
    {
      id: "afd-10", subject: "AFD", topic: "Event Loop",
      question: "What is the main role of Web APIs in the JavaScript runtime?",
      options: ["Execute synchronous JavaScript code", "Provide background threads for tasks like DOM events, timers, and fetch", "Manage the Call Stack", "Allocate memory for variables"],
      correct: 1,
      explanation: "Web APIs (provided by the browser or Node.js) run tasks like setTimeout, fetch, and DOM events off the main thread. When done, their callbacks are placed in the task queue for the Event Loop to pick up.",
      reference: AFD_REF["Event Loop"],
    },
    {
      id: "afd-11", subject: "AFD", topic: "Event Loop",
      question: "How does Node.js handle concurrent requests despite being single-threaded?",
      options: ["It spawns a new thread for every request", "Event-Driven, Non-Blocking I/O model powered by the Event Loop", "It uses multiple CPUs by default", "It forces requests to wait in line synchronously"],
      correct: 1,
      explanation: "Node.js uses libuv's event loop and async I/O. While waiting for I/O (DB queries, file reads, network), Node continues processing other requests. Callbacks resume when I/O completes — no thread-per-request needed.",
      reference: AFD_REF["Event Loop"],
    },
    {
      id: "afd-12", subject: "AFD", topic: "Auth",
      question: "Why is a JSON Web Token (JWT) considered 'stateless'?",
      options: ["It requires session storage on the server", "The token itself contains all the information needed to authenticate the user", "It expires quickly", "It is stored in cookies only"],
      correct: 1,
      explanation: "A JWT encodes the user's identity and claims in the token payload itself (header.payload.signature). The server verifies the signature without needing to look up a session in a database — hence stateless.",
      reference: AFD_REF["Auth"],
    },
    {
      id: "afd-13", subject: "AFD", topic: "useState",
      question: "With const [count, setCount] = useState(0), how do you safely increment using previous state?",
      options: ["setCount(count + 1)", "setCount(prev => prev + 1)", "count = count + 1", "setCount(1)"],
      correct: 1,
      explanation: "State updates may be batched. Using setCount(prev => prev + 1) ensures you always increment from the latest state, not a stale closure value. This is especially important inside event handlers and useEffect.",
      reference: AFD_REF["useState"],
    },
    {
      id: "afd-14", subject: "AFD", topic: "React Core",
      question: "Which array method is most commonly used in React to render a list of components?",
      options: [".filter()", ".reduce()", ".forEach()", ".map()"],
      correct: 3,
      explanation: ".map() transforms each array element into JSX and returns a new array of elements. React can render arrays of JSX elements directly. Always add a unique key prop when mapping.",
      reference: AFD_REF["React Core"],
    },
    {
      id: "afd-15", subject: "AFD", topic: "useEffect",
      question: "What happens if you call useEffect with NO dependency array at all?",
      options: ["It runs only once on mount", "It runs after every single render", "It never runs", "It throws a syntax error"],
      correct: 1,
      explanation: "Omitting the dependency array (not passing it at all) means useEffect runs after EVERY render — both the initial mount and every subsequent re-render. This can cause infinite loops if the effect triggers a state update.",
      reference: AFD_REF["useEffect"],
    },
  ],

  FOML: [
    {
      id: "foml-1", subject: "FOML", topic: "Regression",
      question: "In the Simple Linear Regression equation y = β₀ + β₁x, what does β₁ represent?",
      options: ["The y-intercept value", "The slope of the regression line", "The dependent variable", "The number of samples"],
      correct: 1,
      explanation: "β₁ is the slope — it represents how much y changes for each one-unit increase in x. β₀ is the intercept (value of y when x = 0).",
      reference: FOML_REF["Regression"],
    },
    {
      id: "foml-2", subject: "FOML", topic: "Gradient Descent",
      question: "What is the primary purpose of a Loss Function in machine learning?",
      options: ["To increase model complexity", "To measure how far predicted values are from actual values", "To normalize the data", "To create train-test splits"],
      correct: 1,
      explanation: "A loss function quantifies prediction error — it outputs a single number representing how wrong the model's predictions are. Gradient descent then minimizes this number by adjusting model parameters.",
      reference: FOML_REF["Gradient Descent"],
    },
    {
      id: "foml-3", subject: "FOML", topic: "Regression",
      question: "What is the key difference between MAE and MSE?",
      options: ["MAE uses absolute values; MSE squares the errors", "MAE is only for classification; MSE is for regression", "They are identical measures", "MSE cannot be negative"],
      correct: 0,
      explanation: "MAE = mean of |actual − predicted|. MSE = mean of (actual − predicted)². MSE penalizes large errors more heavily due to squaring. MAE is more robust to outliers. Both are always non-negative.",
      reference: FOML_REF["Regression"],
    },
    {
      id: "foml-4", subject: "FOML", topic: "Regression",
      question: "What does R² = 0.95 indicate about a regression model?",
      options: ["95% of predictions are accurate", "95% of variation in the dependent variable is explained by the model", "The model has a 5% error rate", "95% of data points are outliers"],
      correct: 1,
      explanation: "R² (coefficient of determination) measures the proportion of variance in the dependent variable explained by the model. R² = 0.95 means the model explains 95% of the variance — a very good fit.",
      reference: FOML_REF["Regression"],
    },
    {
      id: "foml-5", subject: "FOML", topic: "Regression",
      question: "Which R² value indicates a perfect regression model?",
      options: ["R² = 0", "R² = 0.5", "R² = 1", "R² = 2"],
      correct: 2,
      explanation: "R² = 1 means the model perfectly predicts every data point (zero residuals). R² = 0 means the model explains no variance (no better than predicting the mean). R² > 1 or < 0 indicates a poor model.",
      reference: FOML_REF["Regression"],
    },
    {
      id: "foml-6", subject: "FOML", topic: "Gradient Descent",
      question: "What does the learning rate (α) control in Gradient Descent?",
      options: ["The number of iterations", "The direction of parameter updates", "The size of steps taken toward the minimum", "The final value of the parameters"],
      correct: 2,
      explanation: "The learning rate α controls step size: θ = θ − α × gradient. Large α = large steps (risk of overshooting). Small α = tiny steps (slow convergence). Choosing α is critical for training success.",
      reference: FOML_REF["Gradient Descent"],
    },
    {
      id: "foml-7", subject: "FOML", topic: "Gradient Descent",
      question: "If the learning rate is TOO LARGE in Gradient Descent, what happens?",
      options: ["Convergence becomes very slow", "The algorithm may overshoot the minimum or fail to converge", "The model becomes more accurate", "Parameters update more precisely"],
      correct: 1,
      explanation: "A too-large learning rate causes the update step to jump past the minimum, potentially bouncing back and forth or diverging. The loss curve oscillates or increases instead of decreasing smoothly.",
      reference: FOML_REF["Gradient Descent"],
    },
    {
      id: "foml-8", subject: "FOML", topic: "Bias-Variance",
      question: "What does High Bias lead to in a machine learning model?",
      options: ["Overfitting", "Underfitting", "Perfect predictions", "Increased variance"],
      correct: 1,
      explanation: "High bias means the model is too simple to capture the underlying patterns in data. It makes strong, wrong assumptions → underfits → poor performance on both training and test data.",
      reference: FOML_REF["Bias-Variance"],
    },
    {
      id: "foml-9", subject: "FOML", topic: "Bias-Variance",
      question: "A model that learns noise along with actual patterns exhibits?",
      options: ["High Bias, Low Variance", "Low Bias, High Variance", "Both high bias and high variance", "Neither bias nor variance"],
      correct: 1,
      explanation: "A model with low bias fits training data very closely (even noise), but generalizes poorly to new data (high variance). This is overfitting — the model memorizes rather than learns.",
      reference: FOML_REF["Bias-Variance"],
    },
    {
      id: "foml-10", subject: "FOML", topic: "Bias-Variance",
      question: "What is the goal of the Bias-Variance Tradeoff?",
      options: ["Maximize bias and minimize variance", "Find a balance where both bias and variance are reasonably low", "Maximize variance to capture all patterns", "Eliminate bias completely"],
      correct: 1,
      explanation: "Reducing bias (by making the model more complex) tends to increase variance, and vice versa. The goal is the sweet spot — a model complex enough to learn patterns but not so complex it learns noise.",
      reference: FOML_REF["Bias-Variance"],
    },
    {
      id: "foml-11", subject: "FOML", topic: "Probability",
      question: "Which of the following is an example of a DISCRETE random variable?",
      options: ["Height of a person (in cm)", "Temperature of a room", "Number of heads when tossing 2 coins", "Time taken to complete a task"],
      correct: 2,
      explanation: "A discrete RV takes countable values: {0, 1, 2} heads from 2 coin flips. Height, temperature, and time are continuous — they can take any value in a range (uncountably infinite possible values).",
      reference: FOML_REF["Probability"],
    },
    {
      id: "foml-12", subject: "FOML", topic: "Probability",
      question: "What property must a valid Probability Mass Function (PMF) satisfy?",
      options: ["Sum of all probabilities equals 0", "Sum of all probabilities equals 1", "All probabilities must be greater than 1", "Probabilities can be negative"],
      correct: 1,
      explanation: "A PMF is valid if: (1) P(X=x) ≥ 0 for all x, and (2) Σ P(X=x) = 1 over all possible values. This ensures the total probability of all outcomes sums to certainty.",
      reference: FOML_REF["Probability"],
    },
    {
      id: "foml-13", subject: "FOML", topic: "Probability",
      question: "For a continuous random variable, what must equal 1?",
      options: ["The maximum probability", "The sum of all probabilities", "The total area under the PDF curve", "The standard deviation"],
      correct: 2,
      explanation: "For continuous distributions, individual point probabilities are zero. Instead, probability = area under the PDF curve over an interval. The total area over all values must equal 1.",
      reference: FOML_REF["Probability"],
    },
    {
      id: "foml-14", subject: "FOML", topic: "NumPy",
      question: "What does ndim = 2 represent in a NumPy array?",
      options: ["A 1D vector", "A 2D matrix", "A scalar value", "A 3D tensor"],
      correct: 1,
      explanation: "ndim is the number of dimensions (axes). ndim=1 is a 1D vector (e.g. [1,2,3]). ndim=2 is a 2D matrix (e.g. [[1,2],[3,4]]). ndim=3 is a 3D tensor (e.g. image with height×width×channels).",
      reference: FOML_REF["NumPy"],
    },
    {
      id: "foml-15", subject: "FOML", topic: "Pandas",
      question: "Which of the following file formats can Pandas NOT read natively?",
      options: ["CSV (.csv)", "Excel (.xlsx)", "JSON (.json)", "Binary image files (.jpg, .png)"],
      correct: 3,
      explanation: "Pandas can read CSV, Excel, JSON, SQL, Parquet, HTML, and many other structured formats. Binary image files are not tabular data and cannot be read with pd.read_*(). Use PIL/OpenCV for images.",
      reference: FOML_REF["Pandas"],
    },
  ],

  DBMS: [
    {
      id: "dbms-1", subject: "DBMS", topic: "MongoDB Foundations",
      question: "MongoDB internally stores data in which format?",
      options: ["JSON", "BSON", "XML", "CSV"],
      correct: 1,
      explanation: "MongoDB stores data as BSON (Binary JSON) — a binary-encoded serialization of JSON-like documents. BSON supports additional data types not in JSON (like Date, ObjectId, int32/int64) and is more efficient to parse.",
      reference: DBMS_REF["MongoDB Foundations"],
    },
    {
      id: "dbms-2", subject: "DBMS", topic: "MongoDB CRUD",
      question: "Which statement is TRUE about insertOne() in MongoDB?",
      options: ["Inserts multiple documents", "Inserts only one document", "Replaces an existing document", "Deletes a document"],
      correct: 1,
      explanation: "insertOne({}) inserts exactly one document into a collection. For multiple documents use insertMany([{}, {}]). insertOne returns an acknowledgment with the new _id.",
      reference: DBMS_REF["MongoDB CRUD"],
    },
    {
      id: "dbms-3", subject: "DBMS", topic: "MongoDB CRUD",
      question: "Which MongoDB operator is used for 'less than' in a query?",
      options: ["$lt", "$gt", "$eq", "$ne"],
      correct: 0,
      explanation: "$lt = less than, $gt = greater than, $lte = less than or equal, $gte = greater than or equal, $eq = equals, $ne = not equals. Example: db.products.find({price: {$lt: 100}})",
      reference: DBMS_REF["MongoDB CRUD"],
    },
    {
      id: "dbms-4", subject: "DBMS", topic: "MongoDB CRUD",
      question: "Which MongoDB method updates multiple matching documents?",
      options: ["updateMany()", "updateAll()", "modifyMany()", "changeMany()"],
      correct: 0,
      explanation: "updateMany(filter, update) updates ALL documents matching the filter. updateOne() updates only the first matching document. Always use $set, $inc, etc. as the update operator.",
      reference: DBMS_REF["MongoDB CRUD"],
    },
    {
      id: "dbms-5", subject: "DBMS", topic: "MongoDB Performance",
      question: "What is the primary purpose of $unwind in MongoDB aggregation?",
      options: ["Combine documents from two collections", "Deconstruct an array field into one document per array element", "Join two collections", "Sort documents by a field"],
      correct: 1,
      explanation: "$unwind takes a document with an array field and outputs one document per element in that array. Example: a document with tags: ['a','b','c'] becomes three documents, each with one tag value.",
      reference: DBMS_REF["MongoDB Performance"],
    },
    {
      id: "dbms-6", subject: "DBMS", topic: "MongoDB Performance",
      question: "Which index is automatically created by default in every MongoDB collection?",
      options: ["_id Index", "Primary Index", "Main Index", "Unique Index"],
      correct: 0,
      explanation: "MongoDB automatically creates a unique index on the _id field for every collection. This cannot be dropped. The _id is an ObjectId by default — a 12-byte unique identifier.",
      reference: DBMS_REF["MongoDB Performance"],
    },
    {
      id: "dbms-7", subject: "DBMS", topic: "MongoDB Performance",
      question: "Which aggregation stage is executed FIRST in pipeline optimization for best performance?",
      options: ["$group", "$sort", "$match", "$project"],
      correct: 2,
      explanation: "$match should be placed first to filter documents early, reducing the number of documents that flow through subsequent stages. This is equivalent to a WHERE clause — filter before you group or sort.",
      reference: DBMS_REF["MongoDB Performance"],
    },
    {
      id: "dbms-8", subject: "DBMS", topic: "SQL Advanced",
      question: "What does GROUP BY do in a SQL query?",
      options: ["Sorts the result set", "Groups rows that have the same values in specified columns", "Limits the number of returned rows", "Filters individual rows"],
      correct: 1,
      explanation: "GROUP BY groups rows with identical values in the specified column(s) into summary rows. It's used with aggregate functions (COUNT, SUM, AVG, MAX, MIN) to compute one result per group.",
      reference: DBMS_REF["SQL Advanced"],
    },
    {
      id: "dbms-9", subject: "DBMS", topic: "Transactions",
      question: "What does COMMIT do in a database transaction?",
      options: ["Undoes all changes since last commit", "Reverses the transaction", "Cancels the current operation", "Saves all changes permanently to the database"],
      correct: 3,
      explanation: "COMMIT makes all changes in the current transaction permanent and visible to other users. After COMMIT, the changes cannot be undone with ROLLBACK. It ends the transaction successfully.",
      reference: DBMS_REF["Transactions"],
    },
    {
      id: "dbms-10", subject: "DBMS", topic: "Transactions",
      question: "What does 'Atomicity' mean in the context of ACID properties?",
      options: ["Data remains consistent after the transaction", "Concurrent transactions don't interfere", "Committed data persists through crashes", "All operations in a transaction succeed or ALL are rolled back"],
      correct: 3,
      explanation: "Atomicity means a transaction is treated as a single unit — it either completes fully (all operations succeed) or not at all (if any operation fails, all changes are rolled back). There's no partial completion.",
      reference: DBMS_REF["Transactions"],
    },
    {
      id: "dbms-11", subject: "DBMS", topic: "SQL Basics",
      question: "Which SQL clause limits the number of records returned?",
      options: ["LIMIT", "RESTRICT", "TOP", "FETCH"],
      correct: 0,
      explanation: "LIMIT n is standard SQL (MySQL, PostgreSQL, SQLite) to return only the first n rows. SQL Server uses TOP n. Standard SQL also supports FETCH FIRST n ROWS ONLY. LIMIT is the most common syntax.",
      reference: DBMS_REF["SQL Basics"],
    },
    {
      id: "dbms-12", subject: "DBMS", topic: "SQL Basics",
      question: "Which SQL keyword removes duplicate rows from the result set?",
      options: ["DISTINCT", "UNIQUE", "FILTER", "SORT"],
      correct: 0,
      explanation: "SELECT DISTINCT col FROM table returns only unique values of col, eliminating duplicate rows. UNIQUE is used in table constraints (UNIQUE index), not in SELECT queries.",
      reference: DBMS_REF["SQL Basics"],
    },
    {
      id: "dbms-13", subject: "DBMS", topic: "SQL Advanced",
      question: "Which SQL JOIN returns ONLY rows that have matching values in BOTH tables?",
      options: ["INNER JOIN", "LEFT JOIN", "RIGHT JOIN", "FULL OUTER JOIN"],
      correct: 0,
      explanation: "INNER JOIN returns rows where the join condition is met in both tables. LEFT JOIN returns all rows from the left table plus matches from the right (NULLs if no match). FULL JOIN returns all rows from both.",
      reference: DBMS_REF["SQL Advanced"],
    },
    {
      id: "dbms-14", subject: "DBMS", topic: "SQL Basics",
      question: "Which SQL keyword sorts query results in descending order?",
      options: ["DESC", "ASC", "DOWN", "REVERSE"],
      correct: 0,
      explanation: "ORDER BY col DESC sorts results from largest to smallest (Z→A, 9→0, newest→oldest). ASC (default) sorts ascending. Example: SELECT * FROM students ORDER BY score DESC LIMIT 5;",
      reference: DBMS_REF["SQL Basics"],
    },
    {
      id: "dbms-15", subject: "DBMS", topic: "MongoDB Performance",
      question: "Which MongoDB aggregation stage performs a JOIN-like operation between two collections?",
      options: ["$lookup", "$join", "$merge", "$link"],
      correct: 0,
      explanation: "$lookup performs a left outer join between the current collection and another collection in the same database. It adds an array field to each document containing matching documents from the joined collection.",
      reference: DBMS_REF["MongoDB Performance"],
    },
  ],
};

export const ALL_SUBJECTS = ["DSA", "AFD", "FOML", "DBMS"];

export const SUBJECT_META = {
  DSA: {
    label: "DSA",
    full: "Data Structures & Algorithms",
    color: "#e63946",
    topics: ["Big-O", "Recursion", "Stack & Queue", "Trees & BST", "Sorting", "Two-Pointer"],
  },
  AFD: {
    label: "AFD",
    full: "Advanced Frontend Development",
    color: "#2563eb",
    topics: ["React Hooks", "State & Props", "React Router", "Express & Node", "Event Loop", "Auth & JWT"],
  },
  FOML: {
    label: "FOML",
    full: "Foundations of Machine Learning",
    color: "#16a34a",
    topics: ["Linear Regression", "Gradient Descent", "Bias-Variance", "Probability", "NumPy & Pandas"],
  },
  DBMS: {
    label: "DBMS",
    full: "Database Management Systems",
    color: "#7c3aed",
    topics: ["MongoDB CRUD", "Aggregation", "SQL Queries", "Joins", "Transactions & ACID"],
  },
};
